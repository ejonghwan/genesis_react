import React from 'react';

const MainVisual = () => {
    return (
        <div style={{ background: "black", height: "500px" }}>
            main V
        </div>
    );
};

export default MainVisual;